mixin isLoggedInTemplate(name)
    .is-logged-in-wrapper
        if isLoggedIn
            div(class='badge badge-success')
                span(class='badge-label') Hello #{name}
        else
            div(class='badge badge-danger')
                span(class='badge-label') Unknown user
            