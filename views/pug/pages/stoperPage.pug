extends ../templates/pageWithNavigation
include ../mixins/stoper.pug
include ../mixins/navigation.pug

block navigation
    +navigation(navigations, navigationTitle)
block pageTitle
    h2 Stop watch

block content
    //- +stoper(hours, minutes, secundes, miliseconds)
    +stoper(h, m, s, ms)
    

