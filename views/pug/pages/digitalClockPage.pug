extends ../templates/pageWithNavigation
include ../mixins/digitalClock.pug
include ../mixins/navigation.pug
include ../mixins/isLoggedIn.pug

block navigation
    +navigation(navigations, navigationTitle)

block pageTitle
    h2 Digital clock
    div.aside-right
        +isLoggedInTemplate(name)


block content
    +digitalClock(hours, minutes, secundes)
